__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.env
.env.*
!.env.example
.pytest_cache/
.mypy_cache/
.ruff_cache/
.contract-gen-recovery/
htmlcov/
.coverage
*.log
.DS_Store

# Cross-chat coordination mailbox. Local only, never published.
_buzon/
buzon/

# Local build scratch: candidate distributions and generated contract trees.
# Regenerated by the build; never part of the published distribution.
.candidate-*/
.contract-final-generated*/

# Self-referential git worktrees created by local tooling. Committing one
# stores a gitlink with no .gitmodules entry, which makes `git submodule
# status` abort before it reaches the real `contracts` submodule.
.codex-work/
